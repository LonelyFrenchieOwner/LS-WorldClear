Config = {}

-- Command/Perms
Config.CommandName        = 'worldclear'
Config.UseAcePerms        = true
Config.RequiredAceGroup   = 'admin' -- add_ace group.admin worldclear.use allow

-- Clear Configuration
Config.ClearCountdown    = true     -- Should there be a countdown? (If not, the clear takes place immediately)
Config.AnnounceInChat     = true -- Should it annouce it in chat?
Config.ClearTime       = 5      -- How long until the clear happens
Config.UseNuiNotify       = true -- Does it use the NUI notification?
